<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chf.upf.myapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".Stats"></activity>
        <activity android:name=".StudyActivity" />
        <activity android:name=".ConfigurationActivity" />
        <activity android:name=".vista_semana" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".SeeTask" />
        <activity android:name=".AddTask" />
        <activity android:name=".registerActivity" />
        <activity android:name=".compartir1" />
        <activity android:name=".compartir2" />
        <activity android:name=".vistaHoy" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>